<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="com.emm.gema.feature.attendance.AttendanceFileProvider"
            android:authorities="${applicationId}.attendance"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/attendance_file_paths" />
        </provider>
    </application>

</manifest>
